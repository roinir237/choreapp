define([
	'jquery',
	'underscore',
  'backbone'
	], 
	
	function($,_, Backbone){
  		var UserCollection = Backbone.Collection.extend({
    			model:User.model
  		});
  	
		return UserCollection;
	}
);
